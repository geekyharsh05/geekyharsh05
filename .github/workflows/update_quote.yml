name: Update Quote
on:
  schedule:
      - cron: "0 0 * * *"
jobs:
  update_quote:
    runs-on: ubuntu-latest
    steps:
      - name: Set up the time zone
        run: |
          sudo ln -sf /usr/share/zoneinfo/$TIME_ZONE /etc/localtime
          echo $TIME_ZONE | sudo tee /etc/timezone
          date

      - name: Configure Git
        run: |
          git init
          git config --global user.email "hp424805@gmail.com"
          git config --global user.name "geekyharsh05"

      - name: Check out repository
        uses: actions/checkout@v2
        with:
          repository: geekyharsh05/geekyharsh05

      - name: Run script
        run: |
          chmod +x quotes.sh
          ./quotes.sh

      - name: Check README.md status
        run: |
          git status README.md

      - name: Commit and push changes
        run: |
             git pull origin main
             chmod +x quotes.sh
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
